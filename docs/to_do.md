# To Do

## Baseline

    1.1 Set up a standard BERT model
    1.2 Greedy sampling strategy - Integrate an early stopping mechanism (evaluate the model on the development set every 15 steps and halt training if the strict span-level F1 score fails to improve for 5 consecutive checks)

## Implement Order-Agnostic Data Augmentation (OADA)

Address data scarcity by treating entity extraction as a set-generation problem rather than a strict left-to-right sequence. Permutations of entity types ([PER, LOC, ORG]) as ordering instructions to rearrange the entities into unique target sequences.

    2.1 Add entity extraction utility that works with current dataset rows
    2.2 Generate Ordering Instructions. CoNLL-2003 has 4 entity types [PER, LOC, ORG, MISC] - 24 possible permutations (4!=24)
    2.3 Group and Rearrange Entities (Target Sequence)
    2.4 Construct Unique Input-Output Pairs

## Adapt BERT using Pattern-Exploiting Training (PET)

Reformulate the NER task inputs into cloze-style phrases (fill-in-the-blank questions)

## Integrate the Novel Hybrid Architecture

Combine OADA and PET

## Answer research question

Compare metrics generated by hybrid OADA-PET model against the baseline
