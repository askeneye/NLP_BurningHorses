# To Do

## Baseline
    1.1 Set up a standard BERT model
    1.2 Greedy sampling strategy - Integrate an early stopping mechanism (evaluate the model on the development set every 15 steps and halt training if the strict span-level F1 score fails to improve for 5 consecutive checks)

## Implement Order-Agnostic Data Augmentation (OADA)
    2.1 Address data scarcity by treating entity extraction as a set-generation problem rather than a strict left-to-right sequence.Permutations of entity types ([PER, LOC, ORG]) as ordering instructions to rearrange the entities into unique target sequences.

## Adapt BERT using Pattern-Exploiting Training (PET)
    3.1 Reformulate the NER task inputs into cloze-style phrases (fill-in-the-blank questions)

## Integrate the Novel Hybrid Architecture
    4.1 Combine OADA and PET 

## Answer research question
    5.1 Compare metrics generated by hybrid OADA-PET model against the baseline 

