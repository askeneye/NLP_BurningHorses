# Plots

Place plot-ready CSVs and rendered report figures here.

Each final figure should have:

- A plot-ready CSV containing exactly the data shown.
- A rendered PNG or PDF.
- A script in `../scripts/` that regenerates the output.

Use `../styleguide.md` for figure sizes, labels, colors, and placeholder styling.
