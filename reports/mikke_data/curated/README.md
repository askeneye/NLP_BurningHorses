# Curated Data

Place normalized report-facing CSV files here.

Curated files should follow `../data_dictionary.md` and should make score status explicit. A later compiler script can populate this folder from raw artifacts in `results/`, `reports/`, or `data/interim/`.

Do not put raw experiment logs or large JSONL prediction dumps here unless they are intentionally reduced to report-facing CSV form.
